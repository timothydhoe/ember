# Alacritty

Three files, one per contrast level — pick the one you want:

```toml
[general]
import = ["/absolute/path/to/alight-medium.toml"]  # or alight-hard.toml / alight-soft.toml
```

Verify: `uv run scripts/live_show_swatches.py` from `alight/`

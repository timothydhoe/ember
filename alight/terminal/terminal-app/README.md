# Terminal.app

Three profiles, one per contrast level:

1. Double-click `alight-medium.terminal` (or `alight-hard.terminal` / `alight-soft.terminal`)
2. Settings -> Profiles -> the one you imported -> Default
3. Open a new window

Verify: `uv run scripts/live_show_swatches.py` from `alight/`
